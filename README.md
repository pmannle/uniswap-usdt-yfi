# uniswap-usdt-yfi
In node env, swap between YFI and USTD using uniswap

```npm install```

Create a .env file with the following:
```
INFURA_URL=
PRIVATE_KEY=
WALLET_ADDRESS=
```

Then:

```node uniswap.js```
